# Скопируй в корень проекта как .env.production:
#   cp deploy/.env.production.example .env.production
#
# docker compose --env-file .env.production -f docker-compose.prod.yml up -d --build

# Публичный URL сайта (для CORS). Без слэша в конце.
CORS_ORIGINS=http://93.77.164.66

# Сгенерируй: openssl rand -hex 32
JWT_SECRET=замени_на_случайную_строку_минимум_32_символа

POSTGRES_USER=postgres
POSTGRES_PASSWORD=смени_пароль_бд
POSTGRES_DB=harbor_db
